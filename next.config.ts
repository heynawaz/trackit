/**
 * Run `yarn dev` or `yarn build` with `SKIP_ENV_VALIDATION=true` to skip env checks—
 * handy for Docker or CI images.
 */
import './env';

import type { NextConfig } from 'next';

const nextConfig: NextConfig = {

};

export default nextConfig;